server:
  port: 8082

spring:
  application:
    name: MEASUREMENT-SERVICE

  datasource:
    url: jdbc:mysql://quantitydb.cdsye0kq0jvr.ap-southeast-2.rds.amazonaws.com
    username: root
    password: Abhay@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/
    register-with-eureka: true
    fetch-registry: true